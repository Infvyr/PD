import styles from '@pd-frontend/modules/booking/plan/Body/dates.module.css';

export const ChooseDates = () => (
	<div className={`${styles.wrapper} ${styles.chooseDates}`}>
		Choose the dates
	</div>
);
